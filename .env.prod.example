# Copy to .env.prod and fill secrets before deploying.
# docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
# POSTGRES_PASSWORD must be alphanumeric only (no @ : / #).

DOCKERHUB_USERNAME=erfan74
POSTGRES_PASSWORD=changeme
JWT_SECRET=changeme
JWT_EXPIRES_IN=7d
WEB_PORT=80
