# Portainer / compose env for docker-compose.prod.yml
# docker network create proxy-network
# Join NPM to PROXY_NETWORK, then NPM → frontend:4000

DOCKERHUB_USERNAME=erfan74
PROXY_NETWORK=proxy-network
IMAGE_TAG_BACKEND=latest
IMAGE_TAG_FRONTEND=latest
DB_USER=postgres
DB_NAME=building_accounting
DB_PASSWORD=changeme
JWT_SECRET=changeme
JWT_EXPIRES_IN=7d
BACKEND_PORT=5000
FRONTEND_PORT=4000
